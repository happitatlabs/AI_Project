# SQL Analytics Demo

`sql_analytics`는 공통 엔진 위에 올라가는 대표 실행 모듈 데모입니다.  
5분 안에 `/ui -> /user-console` 흐름을 보여주는 시연 경로로 쓰면 됩니다.

## 데모 목표

- `엔진 + 모듈` 구조를 바로 이해시킨다.
- 사용자는 `/ui`에서 모듈만 고르고 실행을 시작한다는 점을 보여준다.
- 모든 실행은 `run_id`로 추적되고, 결과 확인은 공통 콘솔에서 이뤄진다는 점을 보여준다.

## 5분 시연 경로

1. `/ui` 접속
2. `SQL Analytics` 모듈 선택
3. 자연어 질문 입력
4. 실행 시작
5. `/user-console?run_id=...`로 이동
6. 상태, 진행률, 최근 activity 확인
7. 결과 요약과 출력 데이터 확인

## 추천 시연 문구

예시 질문:

- `지난 30일 동안 제품별 매출 합계를 보여줘`
- `가장 반품률이 높은 카테고리 5개를 알려줘`
- `월별 주문 수와 평균 주문 금액을 비교해줘`

시연 포인트:

- 시작점은 모듈 UI지만, 추적 축은 항상 `run_id`
- `/runs`에서 같은 실행으로 다시 들어올 수 있음
- `/user-console`에는 결과만 보이고 raw debug 정보는 숨겨짐
- 운영자와 개발자는 같은 run을 `/operator-console`, `/dev-console`에서 다른 관점으로 봄

## 한 줄 설명

`sql_analytics`는 자연어 질문을 규칙/템플릿 기반 SQL 실행 흐름으로 바꾸고, 그 결과를 공통 실행 콘솔에서 추적 가능한 run으로 제공하는 모듈입니다.
