CREATE TABLE TN_BKCHIT (
    CHK VARCHAR2(30),
    AC_DATE VARCHAR2(8),
    AC_CHITNO VARCHAR2(20),
    OCCR_PART VARCHAR2(40),
    MNEY_UNIT VARCHAR2(5),
    CHIT_AMT NUMBER,
    TR_DATE VARCHAR2(8),
    TR_DATE_SEQ NUMBER
);

CREATE UNIQUE INDEX TN_BKCHIT0 ON TN_BKCHIT (CHK, AC_DATE, AC_CHITNO, OCCR_PART);
