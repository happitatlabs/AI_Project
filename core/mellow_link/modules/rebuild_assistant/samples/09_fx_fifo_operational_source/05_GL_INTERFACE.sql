CREATE TABLE GL_INTERFACE (
    ACCOUNT_DATE VARCHAR2(8),
    REFERENCE4 VARCHAR2(50),
    REFERENCE6 VARCHAR2(50),
    USER_JE_CATEGORY_NAME VARCHAR2(25),
    CURRENCY_CODE VARCHAR2(5),
    ENTERED_DR NUMBER,
    ENTERED_CR NUMBER
);

CREATE UNIQUE INDEX GL_INTERFACE0 ON GL_INTERFACE (ACCOUNT_DATE, REFERENCE4, REFERENCE6);
