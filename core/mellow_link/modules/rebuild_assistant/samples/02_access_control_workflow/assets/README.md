# Assets

이 디렉터리는 `02_access_control_workflow` 샘플의 실제 입력 자산을 넣는 위치다.

권장 자산:

- 권한 분기 로직이 포함된 code
- 승인 흐름 SQL 또는 query
- UI action 또는 화면 자산
- schema
- supporting docs

자산을 채운 뒤 `input_manifest.json`과 `expected_assertions.yaml`의 provisional anchor를 measured 값으로 갱신한다.
