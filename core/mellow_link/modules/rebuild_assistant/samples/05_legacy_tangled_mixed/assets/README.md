# Assets

이 디렉터리는 `05_legacy_tangled_mixed` 샘플의 실제 입력 자산을 넣는 위치다.

권장 자산:

- 여러 책임이 혼합된 code
- SQL 및 query
- UI 또는 handler 자산
- schema
- supporting docs

최악 구조 샘플이므로 boundary mismatch, mixed responsibility, validation leak를 함께 드러내는 자산을 우선 채운다.
