# Assets

이 디렉터리는 `01_crud_simple` 샘플의 실제 입력 자산을 넣는 위치다.

예상 자산 종류:

- code
- sql
- ui
- schema
- supporting docs

현재는 asset이 아직 채워지지 않았고, 상위 `input_manifest.json`과 `expected_assertions.yaml`은 provisional 상태다.
