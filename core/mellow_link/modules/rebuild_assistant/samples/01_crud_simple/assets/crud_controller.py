# CRUD simple sample route map
ROUTES = [
    "reports_list",
    "reports_detail",
    "reports_create",
    "reports_edit",
    "reports_remove",
]

# This file intentionally keeps logic thin so the sample stays close to a
# straightforward CRUD flow instead of a validation-heavy legacy path.
